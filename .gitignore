node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.env.e2e
.auth-state.json
cypress/screenshots/
cypress/videos/
test-results/

# Internal documentation and workflows
AGENTS.md
NPM_PUBLISHING_PLAN.md
tasks/

# MOS-607 task + verification files are tracked even though tasks/ is
# generally ignored — they document the live-meeting verification work
# and need to be reviewable in PRs alongside the code changes.
!Tasks/backlog/MOS-607*.md

# Rust build output
target/
native/target/

# napi-rs generated files
*.node
